programa {
  funcao inicio() {
    inteiro a, b, c
    escreva("digite os valores dos lados do triangulo:")
    leia(a,b,c)

    se ((a > b + c  e b < c )) { escreva("é escaleno")}
    senao { se( a == b= == c) {escreva("é equilatero")}}
    
  }
}
